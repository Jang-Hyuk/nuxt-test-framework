// const Joi = require('joi');

// const validationUtil = require('../validation');

describe('validation.js test', () => {
	test.skip('validate', () => {
		// console.log(Joi.assert('aaa', validationUtil.isMemberId));
		// expect(() => Joi.assert('aaa', validationUtil.isMemberId)).toThrow();
		// expect(() => Joi.assert('aaaa', validationUtil.isMemberId)).not.toThrow();
		// expect(validationUtil.isValidate('aaa', validationUtil.isMemberId)).toBeFalsy();
		// expect(validationUtil.isValidate('aaaa', validationUtil.isMemberId)).toBeTruthy();
	});
});
